# ============================================================================
# Added by reply-claw v0.1 for tenant {{tenant_slug}}
# ============================================================================
# Agent credentials and service keys

ANTHROPIC_API_KEY={{anthropic_api_key}}
TURSO_DB_URL={{turso_db_url}}
TURSO_DB_TOKEN={{turso_db_token}}
TELEGRAM_BOT_TOKEN={{telegram_bot_token}}
TELEGRAM_CHAT_ID={{telegram_chat_id}}

# Email Bison workspace API keys
{{#each workspaces}}
EMAIL_BISON_{{account_key_upper}}_API_KEY={{api_key}}
{{/each}}

# Reply-claw configuration
RC_TENANT_SLUG={{tenant_slug}}
RC_TENANT_CONFIG=/app/container/skills/reply-claw/tenants/{{tenant_slug}}/config.json
RC_BISON_REPLIES_DRY_RUN={{#if preferences.dry_run}}1{{else}}0{{/if}}

# Preferences
RC_DAILY_AUTO_SEND_CAP={{preferences.daily_auto_send_cap}}
RC_CONFIDENCE_FLOOR={{preferences.confidence_floor}}
RC_MAX_PER_CYCLE={{preferences.max_per_cycle}}
RC_CLASSIFY_MODEL={{preferences.classify_model}}
RC_DRAFT_MODEL={{preferences.draft_model}}
